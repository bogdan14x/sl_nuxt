# Use TypeScript, not JavaScript.

# Never write raw SQL; always use Drizzle.

# Write unit tests for new logic with Vitest.

# Use SvelteKit for the frontend.

# Use TailwindCSS for styling.

# Never write any code comments.

# Always check context7 when writing new logic and you are not absolutely sure that the patterns you are using are in line with latest best practices for the currenversion of our dependencies.

# NEVER use <slot> in Svelte components because it is deprecated
